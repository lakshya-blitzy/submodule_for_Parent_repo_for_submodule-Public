## Globally Useful gitignores

> _Part of the `.gitignore` templates collection mounted as a Git submodule of the [`is-sorted`](https://github.com/dcousens/is-sorted) package (Source: `.gitmodules`). Initialize with `git submodule update --init --recursive`._

This directory contains globally useful gitignores,
e.g. OS-specific and editor specific.

For more on global gitignores:
<https://help.github.com/en/github/using-git/ignoring-files#configuring-ignored-files-for-all-repositories-on-your-computer>

And a good blog post about 'em:
<http://augustl.com/blog/2009/global_gitignores>
